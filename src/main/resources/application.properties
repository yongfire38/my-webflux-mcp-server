spring.application.name=webmvc-mcp

# Server Port
server.port=9090

# Logging
logging.level.root=INFO
logging.level.com.example.webmvc=DEBUG

# MCP Server Configuration (WebFlux - Reactive)
spring.ai.mcp.server.type=ASYNC

# Weather API Key
weather.api.key=
